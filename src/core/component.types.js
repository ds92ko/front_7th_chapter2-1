/**
 * @typedef {Object} EventBinding
 * @property {string} eventType 이벤트 타입
 * @property {(event: Event) => void} handler 이벤트 핸들러 함수
 */

/**
 * @typedef {Record<string, any>} ComponentProps
 * 컴포넌트의 props 타입
 */

/**
 * @typedef {Record<string, any>} ComponentState
 * 컴포넌트의 state 타입
 */
